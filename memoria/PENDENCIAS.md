# Pendências STAKIM

- Painel v1.1: reescrever `index.html` para ler os `.md` dinamicamente em vez de conteúdo fixo (maior pendência técnica atual — ver Decisão 008).
- Implementar botões reais "Enviar alterações" / "Atualizar projeto" no Painel (depende do item acima).
- Evoluir identidade visual da marca (primeira missão do Diretor Criativo).
- Rodar limpeza de `.git` (`git gc --prune=now` no Terminal do Mac) periodicamente — ver Decisão 008.
